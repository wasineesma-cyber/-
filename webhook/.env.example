# LINE Messaging API (จาก LINE Developers Console)
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
LINE_CHANNEL_SECRET=your_channel_secret_here

# Firebase Admin SDK (ดาวน์โหลด JSON จาก Firebase Console > Project Settings > Service Accounts)
# แปลง JSON เป็น 1 บรรทัด แล้ววางตรงนี้
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"don-note",...}

# Port (Render/Railway จะเซ็ตเองอัตโนมัติ)
PORT=3000
